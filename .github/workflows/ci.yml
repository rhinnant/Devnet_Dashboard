- name: Set IMAGE TAG
  run: echo "IMAGE_TAG=${GITHUB_SHA}" >> $GITHUB_ENV

- name: Build Docker image
  run: |
    docker build -t ghcr.io/rhinnant/django-app:${{ env.IMAGE_TAG }} .

- name: Push Docker image
  run: |
    docker push ghcr.io/rhinnant/django-app:${{ env.IMAGE_TAG }}
