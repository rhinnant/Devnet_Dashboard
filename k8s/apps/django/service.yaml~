apiVersion: v1
kind: Service
metadata:
  name: django-service
  namespace: dev
spec:
  selector:
    app: django-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
